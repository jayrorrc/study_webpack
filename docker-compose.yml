version: '3.2'
services:
  server:
    build:
      context: ./
      dockerfile: dk-server.Dockerfile
    image: jayrorrc/webpack-server:latest
    container_name: web-server
    ports:
      - 3000:3000
    volumes:
      - type: bind
        source: ./server/
        target: /opt/web
    networks:
      - study
  # client:
  #   build:
  #     context: ./
  #     dockerfile: dk-client.Dockerfile
  #   image: jayrorrc/webpack-client:latest
  #   container_name: web-client
  #   ports:
  #     - 8080:8080
  #   volumes:
  #     - type: bind
  #       source: ./client/
  #       target: /opt/web
  #   networks:
  #     - study

networks:
  study: